# Локальные/секретные файлы — не коммитим (в репозитории только .env.example).
.env
docker-compose.override.yml
kv-dump.json

# Локальная тестовая копия без Docker: данные нативного Postgres
.localpg/
